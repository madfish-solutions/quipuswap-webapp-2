import { useCallback } from 'react';

export const useStableswapFarmStake = () => {
  const stableswapFarmStake = useCallback(async () => {
    return;
  }, []);

  return { stableswapFarmStake };
};
